name: Release - Build node release candidate

on:
  workflow_dispatch:
    inputs:
      binary:
        description: Binary to be build for the release
        default: all
        type: string
      weekly_release_tag:
        description: Tag matching the actual release candidate with the format polkadot-weeklyYYYw#-rcX or polkadot-weeklyYYYYw#(-X) (e.g. polkadot-weekly2025w1-rc1)
        type: string
  workflow_call:
    inputs:
      binary:
        description: Binary to be build for the release
        default: all
        type: string
      weekly_release_tag:
        description: Tag matching the actual release candidate with the format polkadot-weeklyYYYYw#-rcX or polkadot-weeklyYYYYw#(-X) (e.g. polkadot-weekly2025w1-rc1)
        type: string

jobs:
  validate-inputs:
    runs-on: parity-default
    outputs:
        weekly_release_tag: ${{ steps.validate_inputs.outputs.weekly_release_tag }}

    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Validate inputs
        id: validate_inputs
        env:
          INPUT_WEEKLY_RELEASE_TAG: ${{ inputs.weekly_release_tag }}
        run: |
          . ./.github/scripts/common/lib.sh

          RELEASE_TAG=$(validate_weekly_release_tag "$INPUT_WEEKLY_RELEASE_TAG")
          echo "weekly_release_tag=${RELEASE_TAG}" >> $GITHUB_OUTPUT

  build-polkadot-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'polkadot' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["polkadot", "polkadot-prepare-worker", "polkadot-execute-worker"]'
      package: polkadot
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-polkadot-parachain-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'polkadot-parachain' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["polkadot-parachain"]'
      package: "polkadot-parachain-bin"
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-polkadot-omni-node-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'polkadot-omni-node' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["polkadot-omni-node"]'
      package: "polkadot-omni-node"
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
      features: runtime-benchmarks
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-frame-omni-bencher-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'frame-omni-bencher' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["frame-omni-bencher"]'
      package: "frame-omni-bencher"
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-chain-spec-builder-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'chain-spec-builder' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["chain-spec-builder"]'
      package: staging-chain-spec-builder
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-substrate-node-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'substrate-node' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["substrate-node"]'
      package: staging-node-cli
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-eth-rpc-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'eth-rpc' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["eth-rpc"]'
      package: pallet-revive-eth-rpc
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-subkey-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'subkey' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["subkey"]'
      package: subkey
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: x86_64-unknown-linux-gnu
    secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-polkadot-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'polkadot' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["polkadot", "polkadot-prepare-worker", "polkadot-execute-worker"]'
      package: polkadot
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-polkadot-parachain-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'polkadot-parachain' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["polkadot-parachain"]'
      package: polkadot-parachain-bin
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-polkadot-omni-node-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'polkadot-omni-node' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["polkadot-omni-node"]'
      package: polkadot-omni-node
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
      features: runtime-benchmarks
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-frame-omni-bencher-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'frame-omni-bencher' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["frame-omni-bencher"]'
      package: frame-omni-bencher
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-chain-spec-builder-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'chain-spec-builder' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["chain-spec-builder"]'
      package: staging-chain-spec-builder
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-substrate-node-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'substrate-node' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["substrate-node"]'
      package: staging-node-cli
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-eth-rpc-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'eth-rpc' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["eth-rpc"]'
      package: pallet-revive-eth-rpc
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read

  build-subkey-macos-binary:
    needs: [validate-inputs]
    if: ${{ inputs.binary == 'subkey' || inputs.binary == 'all' }}
    uses: "./.github/workflows/release-reusable-rc-build.yml"
    with:
      binary: '["subkey"]'
      package: subkey
      weekly_release_tag: ${{ needs.validate-inputs.outputs.weekly_release_tag }}
      target: aarch64-apple-darwin
    # secrets: inherit
    permissions:
      id-token: write
      attestations: write
      contents: read
